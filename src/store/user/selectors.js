export const selectToken = (state) => state.user.token;

export const selectUser = (state) => state.user.profile;

export const selectUserIslands = (state) => state.user.userIslands.islands;

export const selectUserDreamies = (state) => state.user.userDreamies;

export const selectUserResidents = (state) => state.user.userResidents;
